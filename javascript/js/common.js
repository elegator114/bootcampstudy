// js 파일에서 "/**" 이라고 치면 아래 5줄 내용이 정리됨
/**
 *
 * @param {*} productAmount
 * @returns
 */

function taxAmount(productAmount) {
  // 부가세 10% 가정
  const tax = 0.000001;
  return productAmount * tax;
}

function getTop5() {
  return;
}

function aaa() {
  return "aaaaa";
}
